# 🚀 Автозавершение текста на LSTM — руководство пользователя

В рамках данного проекта реализована система автозавершения коротких текстов (твитов) на основе рекуррентной нейросети LSTM, а также инструменты для её обучения, оценки и сравнения с предобученной моделью DistilGPT2.

---

## 📓 Структура Jupyter Notebook и пошаговая инструкция

Jupyter Notebook `notebooks/solution.ipynb` состоит из девяти последовательных ячеек. Запускайте их строго сверху вниз.

### 🔧 1. Настройка путей и глобальных параметров

Первая ячейка настраивает среду выполнения и определяет ключевой параметр работы:

- **`DEBUG_MODE`** — флаг режима работы:
  - `True`: запуск в **отладочном режиме** (малые данные, упрощённая модель, обучение на CPU за 2–5 минут).
  - `False`: запуск в **полном режиме** (все данные, полная модель, обучение на GPU за 10–60 минут).

Эта ячейка также добавляет корень проекта в путь поиска Python-модулей, позволяя импортировать код из папки `src/`.

> **Важно**: после любых изменений в файлах `src/*.py` необходимо **перезапустить ядро** (`Kernel → Restart & Run All`), чтобы изменения вступили в силу.

---

### 🧹 2. Предобработка данных

Ячейка выполняет полный цикл подготовки данных:

- Загружает сырые данные из `data/raw_dataset.csv`.
- Применяет очистку текста: приведение к нижнему регистру, удаление ссылок, упоминаний и спецсимволов.
- В отладочном режиме ограничивает датасет первыми 600 строками.
- Разбивает данные на три выборки: обучающую (80%), валидационную (10%) и тестовую (10%).
- Сохраняет выборки в формате CSV в папку `data/`.

**Формат входного файла**: одна строка = один твит, без заголовков. Запятые внутри твитов допускаются.

---

### ⚙️ 3. Генерация конфигурационного файла

Ячейка автоматически создаёт файл конфигурации `configs/config_active.yaml` на основе значения `DEBUG_MODE`. Конфиг содержит все гиперпараметры модели и обучения, что обеспечивает воспроизводимость экспериментов.

---

### 🏋️ 4. Обучение LSTM-модели

Ячейка запускает процесс обучения:

- Создаёт датасеты и DataLoader на основе подготовленных данных.
- Инициализирует модель с параметрами из конфига.
- Выполняет обучение с ранней остановкой (early stopping) по метрике ROUGE-1.
- Сохраняет лучшую модель и словарь в папку `models/`.
- Возвращает историю обучения для последующей визуализации.

---

### 📈 5. Визуализация результатов обучения

Ячейка строит два графика:

- **График потерь (Loss)** — показывает, как уменьшается функция потерь на обучающей выборке.
- **График ROUGE** — отображает динамику ROUGE-1 и ROUGE-2 на валидационной выборке.

Также выводятся финальные численные значения метрик.

---

### 🧪 6. Финальная оценка на тестовой выборке

Ячейка оценивает качество обученной модели на **невиданных данных** (тестовая выборка):

- В отладочном режиме использует первые 100 примеров.
- Вычисляет ROUGE-1 и ROUGE-2.
- Позволяет объективно судить о способности модели обобщать.

---

### ✍️ 7. Инференс: генерация текста с LSTM

Ячейка демонстрирует работу модели в режиме автозавершения:

- Принимает пользовательский префикс (например, `"hello friends"`).
- Очищает и токенизирует его так же, как при обучении.
- Генерирует продолжение длиной `max_new_tokens`.
- Выводит несколько примеров генерации для визуальной оценки качества.

---

### 🤖 8. Инференс: генерация текста с DistilGPT2

Ячейка выполняет **аналогичную генерацию** с использованием предобученной модели DistilGPT2:

- Использует те же параметры длины контекста и числа генерируемых токенов.
- Позволяет **визуально сравнить** качество генерации твоей модели и базовой модели «из коробки».
- Работает без дообучения — показывает качество zero-shot применения.

---

### 📊 9. Сравнение моделей по метрикам ROUGE

Ячейка проводит **объективное количественное сравнение**:

- Оценивает DistilGPT2 на том же тестовом подмножестве.
- Строит сравнительную таблицу ROUGE-1 и ROUGE-2.
- Позволяет сделать вывод о том, насколько твоя модель адаптирована под домен твитов по сравнению с общей языковой моделью.

---

## ⚙️ Параметры конфигурационного файла

Конфигурация хранится в YAML-файлах (`configs/config_debug.yaml`, `configs/config_full.yaml`). Все параметры разделены на три секции.

### Секция `data`

- **`train_path`, `val_path`** — пути к обучающей и валидационной выборкам.
- **`debug`** — флаг отладочного режима (дублирует `DEBUG_MODE` в ноутбуке).
- **`debug_train_size`, `debug_val_size`** — количество строк для обучения и валидации в отладочном режиме.

### Секция `model`

- **`seq_len`** — длина контекста (число токенов, на основе которых делается предсказание). Оптимально: 8–12 для твитов.
- **`max_vocab_size`** — максимальный размер словаря. Редкие слова заменяются на `<unk>`. Оптимально: 3000 (дебаг), 15000 (полный).
- **`embed_dim`** — размерность эмбеддингов слов. Влияет на выразительную способность модели.
- **`hidden_dim`** — размерность скрытого состояния LSTM. Обычно в 2 раза больше `embed_dim`.
- **`num_layers`** — число слоёв LSTM. 1–2 слоя достаточно для твитов.
- **`dropout`** — вероятность dropout для регуляризации (предотвращения переобучения).

### Секция `training`

- **`batch_size`** — размер батча. На GPU можно использовать 64, на CPU — 16.
- **`lr`** — learning rate (скорость обучения). Стандартное значение для Adam: 0.001.
- **`num_epochs`** — максимальное число эпох обучения.
- **`max_gen_tokens`** — сколько токенов генерировать при оценке и инференсе.
- **`early_stopping_patience`** — сколько эпох ждать улучшения ROUGE-1 перед остановкой обучения.

---

## 📏 Метрики качества: ROUGE-1 и ROUGE-2

Проект использует две ключевые метрики для оценки качества генерации текста.

### 📌 ROUGE-1

- **Что измеряет**: полноту на уровне отдельных слов (унграмм).
- **Интерпретация**: доля слов из реального продолжения, которая присутствует в сгенерированном тексте.
- **Что показывает**: насколько модель улавливает ключевую лексику домена.

### 📌 ROUGE-2

- **Что измеряет**: полноту на уровне последовательных пар слов (биграмм).
- **Интерпретация**: доля биграмм из реального продолжения, которая присутствует в сгенерированном тексте.
- **Что показывает**: насколько модель сохраняет локальный порядок слов и фразовые конструкции.

Обе метрики вычисляются как **F1-мера** (гармоническое среднее точности и полноты), что обеспечивает сбалансированную оценку. Метрики усредняются по множеству примеров (100 в дебаге, 200+ в полном режиме) для статистической достоверности.
